<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ErrorPopupBackground}" />
        <Setter Property="Height" Value="155" />
        <Setter Property="Width" Value="180" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>

    <Style x:Key="ErrorText" TargetType="TextBlock" BasedOn="{StaticResource TextStyle}">
        <Setter Property="Foreground" Value="{StaticResource ForegroundColor}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>

    <Style x:Key="ErrorTitle" TargetType="TextBlock" BasedOn="{StaticResource ErrorText}">
        <Setter Property="FontSize" Value="{StaticResource H3TextSize}" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource App.Button}">
        <Setter Property="Background" Value="{StaticResource ErrorButtonColor}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource TextStyle}">
        <Setter Property="Foreground" Value="{StaticResource ControlsTextBrush}"/>
    </Style>

</ResourceDictionary>